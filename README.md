# rosctl
√¸¡Ó≈‰÷√£∫<br/>
rosctl -u root -p password<br/>
rosctl -uroot -ppassword<br/>
rosctl -u root -p password --auto<br/>
rosctl -uroot -ppassword --auto<br/>
rosctl -u root -p password --ethernet<br/>
rosctl -uroot -ppassword --ethernet<br/>
rosctl -u root -p password --resource<br/>
rosctl -uroot -ppassword --resource<br/>
rosctl -u root -p password --new password<br/>
rosctl -u root -p password --wireless 192.168.1.2<br/>
rosctl -u root -p password --logging 192.168.1.2<br/>
rosctl -u root -p password --snmp 192.168.1.2<br/>
rosctl -u root -p password --ntp 192.168.1.2<br/>
mkatuo -u root -p password --new password --loging 192.168.1.2 --snmp 192.168.1.2<br/>
√¸¡Ó∞Ô÷˙:<br/>
rosctl --help<br/>
rosctl --author<br/>
rosctl --version<br/>
rosctl --copyright<br/>